# eli

A tiny example repository that contains a simple "Hello, world!" web page.

Files:
- `index.html` — a minimal HTML page that displays "Hello, world!".

Usage:
- Open `index.html` in your browser to view the page locally.

Optional:
- To publish this page with GitHub Pages, enable Pages in the repository settings and set the source to the `main` branch (root).

Contributing:
- Feel free to open issues or pull requests to improve the page or add more examples.
